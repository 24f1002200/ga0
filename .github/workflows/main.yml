name: Manual Workflow

on:
  workflow_dispatch: # Enables the manual trigger button

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: 24f1002200@ds.study.iitm.ac.in
        run: echo "Hello World!"
